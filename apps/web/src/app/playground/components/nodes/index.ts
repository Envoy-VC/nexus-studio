import { BridgeNode } from "./bridge";
import { TerminalNode } from "./terminal";
import { TransferNode } from "./transfer";

export const nodeTypes = {
  bridge: BridgeNode,
  terminal: TerminalNode,
  transfer: TransferNode,
};
